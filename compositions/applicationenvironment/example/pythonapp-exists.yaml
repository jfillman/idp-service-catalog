# --required-resources fixture: simulates a real PythonApplication XR named
# "widget-service" already existing in the same namespace as xr-staging.yaml's XR
# (demo-apps) - what 00-app-kind-gate.yaml's own ExtraResources lookup would find for
# a PythonApplication-owned app. Paired with xr-staging.yaml + cluster-registry-ready.yaml
# to prove app-usage.yaml's Usage resolves kind: PythonApplication instead of the
# NodeJSApplication default - see 00-app-kind-gate.yaml's own header for why this
# matters (confirmed live on search-api-kind-prod-prod, same class of bug
# springboot-app-exists.yaml's own fixture already proved for SpringBootApplication).
apiVersion: catalog.idp.io/v1alpha1
kind: PythonApplication
metadata:
  name: widget-service
  namespace: demo-apps
spec:
  devCluster: kind-dev
